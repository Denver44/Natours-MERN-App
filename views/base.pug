doctype html
html
    head
        meta(charset='UTF-8')
        meta(name='viewport' content='width=device-width, initial-scale=1.0')
        link(rel='stylesheet' href='css/style.css') 
        link(rel='shortcut icon' type='image/png' href='img/favicon.png')
        link(href='https://fonts.googleapis.com/css?family=Lato:300,300i,700' rel='stylesheet')
        title Natours | #{title}


    body

        // HEADER
        include _header

        // CONTENT
        block content
            section.overview 
                h1 Welcome
                h1 Page | #{title}
                h1= title

        // FOOTER
        include _footer


//- Indentation is must if you make mistake then output will be different for example content if we don't put the one tab space the content will not be overwritten and get added with that